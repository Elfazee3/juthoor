# Juthoor Fix Loop — State

> Machine-maintained by the fix loop. One task per iteration. Statuses:
> `TODO` · `IN_PROGRESS` · `DONE` · `DONE (LIVE-APPLY PENDING)` · `BLOCKED(reason)`.
> Protocol + acceptance criteria: `docs/FIX_LOOP_BRIEF.md`. Track B detail: `docs/MATCHING_ENGINE_PLAN.md`.

## ⚙️ OPERATING MODE (set 2026-07-11 — every iteration must honor this)

**LOCAL-ONLY / no autonomous production writes.** The user was asked how to handle `[LIVE-APPLY]` tasks and did not confirm autonomous apply, so the loop runs in the safest brief-sanctioned mode (§2.2 fallback):
- Do ALL local work for every task (write migration, `db reset`, pgTAP/tests, commit).
- For `[LIVE-APPLY]` tasks (B2, B3, and any future ones): **do NOT apply to the live DB.** Finish + locally verify + commit the migration, mark the task **`DONE (LIVE-APPLY PENDING)`**, and record the exact apply command in the Live-apply ledger for the user to run.
- Never run `apply_migration` / `db push` against `nlufpicjdeeqcgepewdg`. MCP `execute_sql` read-only introspection is still fine.
- If the user later says "auto-apply is fine," switch to brief §2.2 full mode and drain the pending live-applies.

## Environment status (set at S0; re-check when unblocking Track B)

- **Baseline gates (pre-loop, S0, branch `feat/fix-loop-m0-m3`):** `pnpm typecheck` ✅ clean · `pnpm lint` ✅ 0 warnings / 0 errors (278 files) · `pnpm test` ✅ 88 passed (13 files). **This is the green bar every iteration must preserve.**
- **✅ Docker Desktop came up (~50 min after launch) — local stack now available.** `docker info` → server 29.1.3. Track A (S0, A1–A7) was completed while it booted; **B1 and A8 are now unblocked (reset to TODO)** and the loop continues into Track B.
- **✅ Live Supabase access confirmed via MCP** — project `nlufpicjdeeqcgepewdg` (juthoor), ACTIVE_HEALTHY, **Postgres 17**. `execute_sql` reads the live-only objects, so B1 can introspect and dump them (`pg_get_functiondef` / table DDL). This is read-only on live; B1 writes migration files + verifies with a local `db reset` (no live mutation).
  - **B1 hand-off notes (verified live 2026-07-10):** live-only objects = FUNCTIONS `normalize_arabic(txt text)`, `arabic_phonetic(txt text)`, `search_master_tree(...)` — **its args use a `gender_type` enum; confirm that enum is committed/local before building** — and `compute_degrees(p_source uuid, p_target uuid)`; PLUS the `match_paths` **cache TABLE** (+ its indexes/RLS) — `match_paths` is a table, not a function. Also check `person_names` for generated phonetic columns.
  - **⚠️ Version mismatch:** live is **Postgres 17**; local `apps/database/supabase/config.toml` is `major_version = 15`. Bump the local config to 17 (or verify the dumped objects build on 15) before the "`db reset` builds from scratch" acceptance check.
- **Baseline green bar to preserve:** typecheck clean · lint 0/0 · 111 unit tests (was 88 at S0; Track A added 23).

## Task board (execute in table order, respecting Deps)

| ID | Task | Tags | Deps | Status | Notes |
|----|------|------|------|--------|-------|
| S0 | Bootstrap: create/checkout `feat/fix-loop-m0-m3`; first commit = the untracked planning docs (`docs/MATCHING_ENGINE_PLAN.md`, `docs/FIX_LOOP_BRIEF.md`, `docs/LOOP_STATE.md`); verify Docker + `npx supabase start` + `db reset` + seed villages; record baseline gate results (typecheck/lint/test) | — | — | DONE | Branch ✅, docs committed (d870e0c) ✅, baseline gates ✅ (see Environment status). Docker/supabase verification ✗ — daemon not ready this session (see banner); does not block Track A. |
| A1 | Fix open redirect + silent exchange errors in `auth/callback/route.ts`; encode `next` in `Login.tsx`; unit tests (brief §7-A1) | security | S0 | DONE | New `lib/auth/safeRedirect.ts` sanitizer (rejects off-origin, `//host`, `/\`, control chars, bad encoding) wired into callback + confirm routes; callback now routes exchange failures to `/auth/auth-code-error` (no console); `Login.tsx` encodes `next`. 8 unit tests. Gates green. |
| A2 | OTP length: accept 6–8 digits client-side, default 8, update SUPABASE_OTP_SETUP.md (brief §7-A2) | auth | S0 | DONE | `otpConfig.ts` defaults to 8 + adds `OTP_MIN/MAX_LENGTH` and `isValidOtp()`; both submit gates (Signup, LoginOtpTab) now use `isValidOtp` (accepts 6–8) instead of exact-length; stale comments fixed; +5 unit tests. SUPABASE_OTP_SETUP.md advice corrected in place (NOTE: that doc is at the ancestry root, outside the juthoor repo — updated but not in the commit). Gates green (101 tests). |
| A3 | Password policy min(8) + bilingual error in `security.ts` (brief §7-A3) | auth | S0 | DONE | New reusable `lib/auth/passwordPolicy.ts` (min 8 + bilingual AR·EN message); `security.ts` update-password schema uses it; `UpdatePassword.tsx` onError now surfaces the validation message (not just serverError). +3 unit tests. Existing logins not re-validated → no lockout. Gates green (104 tests). |
| B1 | M0: dump + commit ALL live-only DB objects into a migration; fresh `db reset` builds search/degrees end-to-end (brief §8-M0.1, plan §6 "hard dependency") | matching, db | S0 | DONE | ✅ VERIFIED 2026-07-11: `npx supabase db reset` builds all 27 migrations from scratch (exit 0, only idempotent DROP-IF-EXISTS notices). Confirmed in fresh local DB: 4 functions (normalize_arabic, arabic_phonetic, search_master_tree, compute_degrees), `match_paths` table, person_names generated cols (given/surname _norm/_phonetic) all present; `search_master_tree(...)` executes; `seed_villages.sql` → 456 places; trigram village search works (اللد→1 hit). Built on local PG15 (no config bump needed despite live PG17). Migrations committed in 2fb767a. |
| B2 | M0: replace leaky `matches_select` with admin-only SELECT + pgTAP proving non-admin reads 0 rows (plan §5.4) | security, db, [LIVE-APPLY] | B1 | DONE (LIVE-APPLY PENDING) | Migration `20260711000000_matches_rls_admin_only.sql` drops leaky policy → `matches_select_admin_only USING is_admin()`. pgTAP `tests/matches_rls_test.sql` (4 assertions) proves non-admin public-tree owner reads 0 rows, admin reads 1. Local `db reset` + `supabase test db` green (46/46). NOT applied to live (local-only mode) — see ledger. |
| B3 | M0: degrees-path masking for living/non-permissioned nodes (SQL + `data/user/degrees.ts` + search UI) + pgTAP (plan §5.1) | security, db, [LIVE-APPLY] | B1 | DONE (LIVE-APPLY PENDING) | Migration `20260711010000_degrees_masking.sql`: `is_person_living()` + `mask_degrees_path()` + `compute_degrees()` rewrite (caches STRUCTURAL path, masks per-viewer on return; clears old named cache). Masking enforced at the DB so the app can't ship names — no app change needed (typecheck green, PathNode shape unchanged, masked nodes render the bilingual redacted token). pgTAP `degrees_masking_test.sql` (9) green; full suite 55/55. NOT applied to live. |
| B4 | M0: `app_settings` (auto_merge_enabled=false, 450, 0.78) + `matching_runs` + `person_privacy_holds` + `is_person_living()` + pgTAP (plan §7-M0) | matching, db | B1 | DONE | Migration `20260711020000_matching_m0_foundations.sql`: 3 tables + RLS (app_settings admin-only w/ kill-switch OFF by default; matching_runs admin-read; person_privacy_holds tree-owner-manages). pgTAP (8) green; suite 63/63. is_person_living was in B3. Additive feature schema — deploys with the engine (no urgent live-apply). |
| B5 | M0: CI — remove upstream guard from integration-tests.yml only; add pgTAP job; wire `test-db` into turbo (plan §7-M0) | ci | B1 | DONE | Removed upstream-repo guard (e2e job set `if: false` w/ reason until A8; other workflows' guards untouched). Added unguarded `pgtap` job running `pnpm run test:db` on push/PR. Wired `test-db` into turbo.json + root `test:db` script. Verified: YAML parses (2 jobs); `pnpm run test:db` green 63/63 = the exact CI command. |
| B6 | M0: mask living-person birth-year/origin in public-tree search results (plan §5.5) | security | B1 | DONE | Primary surface fixed: `getTreePeoplePage` (public "List of all people") now hides a living person's birthYear + birthPlaceAr from non-privileged viewers (new pure `lib/privacy/lifeData.ts` `shouldHideLifeData` + `viewerCanWriteTree` owner/approved check). +3 unit tests; typecheck/lint/114 tests green. Secondary surface (search_master_tree) → tracked as B6b. |
| B6b | M0 follow-up: mask living-person birth_year + origin in `search_master_tree` results for non-privileged viewers (v2_3; the RPC already redacts private trees) (plan §5.5) | security, db, [LIVE-APPLY] | B1 | DONE (LIVE-APPLY PENDING) | Migration `20260711030000_search_master_tree_v2_3_living_mask.sql`: `is_privileged` added; birth_year + origin_place_id + place names masked for living non-privileged (name still shown in public). pgTAP `search_living_mask_test.sql` (7) green; suite 70/70. §5.5 fully closed (with B6). NOT applied to live. |
| A4 | Replace 10 uncached `getUser()` sites with cached claims/verified helpers per guide; ≤1 auth round-trip per page (brief §7-A4) | auth, perf | A1 | DONE | Added `getCachedLoggedInUserIdOrNull` helper; 8 uid-only reads (degrees, attachments, places, access, personProfiles, identityVerification ×3, person-360 page) now use cached claims (0 network); 2 admin sites (assertAdmin, admin page) use cached VERIFIED helper (still server-verified, now deduped). Only middleware + the verified helper still call getUser. Person-360 render now does 0 auth round-trips. Gates green (104 tests). |
| A5 | Extract single `requireAdmin()` helper; migrate both duplicate call sites (brief §7-A5) | auth | A4 | DONE | New `data/admin/requireAdmin.ts` (cached `getAdminUserId` + `requireAdmin` + `isCurrentUserAdmin`, server-verified). Removed local `assertAdmin` (identityVerification.ts) and `isCurrentUserAdmin` (admin page); both migrated. Admin page guard + data fetch now share one cached is_admin lookup. Ready for Track B `/admin/review`. Gates green (104 tests). |
| A6 | Delete dead auth code (signUpAction, magic-link action, NewLogin.tsx, getSession helper); fix middleware doc drift (brief §7-A6) | cleanup | A4 | DONE | Verified all 4 symbols unused (grep), then removed: `signUpAction` + `signInWithMagicLinkAction` (auth.ts), `getCachedLoggedInSupabaseUser` (rsc-data), `NewLogin.tsx` (git rm). Fixed SUPABASE_GETCLAIMS doc middleware section → real proxy.ts default-deny allowlist (was stale protectedPages/path-to-regexp). Gates green (104 tests). |
| A7 | Sanitize server-action error messages (bilingual mapper, no raw Supabase internals) (brief §7-A7) | auth | A1 | DONE | New `lib/auth/authErrors.ts` `friendlyAuthError()` maps known auth failures (OTP, credentials, rate-limit, duplicate, auth-required, weak-pw) to bilingual messages; unknown → generic (no leak). `safe-action.ts` logs raw error server-side, returns friendly message. Wrong-OTP UX preserved. +7 unit tests. Gates green (111 tests). |
| B7 | M1: `match_features` + `match_features_dirty` (deny-all RLS) + derivation triggers on all six source tables; dirty queue = sole probe source (plan §6/§7-M1) | matching, db | B1, B4 | DONE | Migration `20260711040000_match_features_layer.sql`: both tables (deny-all RLS) + indexes + `enqueue_match_features_dirty` (person + children + grandchildren + spouses) + AFTER triggers on persons/person_names/events/person_privacy_holds/families/family_children. pgTAP (6) green; suite 76/76. `refresh_match_features` (computation) deferred to B8/B9 (needs transliterate). Additive schema. |
| B8 | M1: `transliterate_to_arabic` ported from `lib/search/phonetic.ts` + `name_variants` seed (plan §6) | matching, db | B1 | DONE | Migration `20260711050000_transliterate_and_name_variants.sql`: `transliterate_to_arabic()` (15-rule Latin→Arabic fallback fold) + `name_variants` dictionary (public-read RLS) seeded. pgTAP `transliterate_test.sql` (7): Ibrahim/Ibraheem/Abraham→ابراهيم, passthrough, pipeline unify. Suite 83/83. `refresh_match_features` → B8b. |
| B8b | M1: `refresh_match_features(person_id)` + `_batch()` — derive the per-person feature vector (transliterate→normalize/phonetic; father/mother/grandparents matching `resolveParents`) + pgTAP parity (plan §6/§7-M1) | matching, db | B7, B8 | DONE | Migration `20260711060000_refresh_match_features.sql`: `match_resolve_parents` (resolveParents parity), `mf_given/surname_norm/phon` helpers, `refresh_match_features` (self+parents+4 grandparents+spouses+life facts, UPSERT + dirty drain), `_batch`. pgTAP `refresh_match_features_test.sql` (8) green; suite 91/91. Feature layer fully functional. |
| B9 | M1: helpers (`name_score`, `year_band_pts`, `cluster_gate`) + authoritative `score_pair` with collapse/cluster-gate/disagreement-vetoes/score_pct + PII-minimized breakdown (plan §4) | matching, db | B7, B8 | DONE | Migration `20260711070000_score_pair.sql`: helpers (`mf_name_pts`, `mf_collapse_pts`, `mf_year_pts`, `mf_place_pts`) + `score_pair` (25 params, literal+phonetic collapse, partial credit, hard vetoes gender/same-tree/Δbirth>15/privacy_hold, living flag, PII-min breakdown). pgTAP `score_pair_test.sql` (7) green; suite 98/98. Refined gate → B9b. |
| B9b | M1: `score_pair` refined gate — evidence-cluster gating (≥2 clusters incl. non-patriline), `score_pct` over clusters, disagreement vetoes (present-but-different mother/spouse/origin/mgp/birth-year), min discriminating fields; set `meta.decision` (plan §4 gates) | matching, db | B9 | DONE | Migration `20260711080000_score_pair_gate.sql`: `mf_present` helper + CREATE OR REPLACE score_pair with clusters (patri/maternal/spouse/anchors), score_pct (capped 1.0), 5 disagreement vetoes, decision=auto_link only when gates pass + reachable threshold. pgTAP `score_pair_gate_test.sql` (6) green; suite 104/104 (B9 core still green). **M1 scorer complete.** |
| B10 | M1: synthetic eval fixtures (incl. cross-script pair, namesake trap, different-spouse veto) + pgTAP `match_engine_test.sql` + Vitest parity incl. U/X `resolveParents` cases; recall floors wired into CI (plan §7-M1 exit) | matching, test | B9, B5 | DONE | pgTAP `match_engine_eval_test.sql` (5): resolveParents U/X + same-gender parity, end-to-end cross-script scoring. Vitest `phonetic.parity.test.ts`: JS↔SQL transliterate parity — **caught + fixed a real bug** (phonetic.ts `/ali/` before `/khalil/` folded Khalil→علي). Namesake trap + spouse/mother disagree already covered in score_pair_gate_test. Suite: pgTAP 109/109, Vitest 131/131. **M1 complete.** |
| B11 | M2: `match_block_keys` + `match_block_skips` + 7 blocking passes + non-name anchor pass + `generate_match_candidates` with caps (plan §7-M2) | matching, db | B7 | DONE | Migration `20260711090000_blocking.sql`: both tables (deny-all) + collision index, `refresh_match_block_keys` (6 passes over folded features incl. cross-script + anchors; privacy_hold excluded), `generate_match_candidates` (cross-tree a<b, skew-capped→match_block_skips, matches anti-join). pgTAP `blocking_test.sql` (6) green; suite 115/115. |
| B12 | M2: `run_matching_batch` (advisory lock, chunked, timeout-0, shadow-forced pending, never clobber human decisions) + pg_cron schedules (guarded) (plan §7-M2) | matching, db | B9, B11 | DONE | Migration `20260711100000_run_matching_batch.sql`: full orchestrator (refresh→block→candidates→score_pair→UPSERT matches as pending), advisory-lock single-flight + timeout-0, never-clobber ON CONFLICT, matching_runs counters. pgTAP `run_matching_batch_test.sql` (6) end-to-end green; suite 121/121. **Matcher demoable.** person_links(proposed) + chunking + pg_cron schedules deferred (M3/scale/ops). |
| B13 | M2: eval schema + `run_eval` + baseline PR curve on fixtures; `matching_runs` counters verified (plan §7-M2 exit) | matching, db | B9, B10 | DONE | Migration `20260711110000_eval_harness.sql`: `eval_pairs` + `eval_runs` (deny-all) + `run_eval(engine_version)` (scores gold set via score_pair, sweeps best-F1 threshold, records precision/recall/F1/cross_script_recall). pgTAP `eval_harness_test.sql` (6) green; suite 127/127. **M2 COMPLETE.** |
| B14 | M3: `match_audit`, `notifications`, `contact_relay`, `match_hints`, `match_review_cards` view (masked), `person_identity_groups` matview + unique index, `v_match_explanations` (plan §7-M3) | matching, db | B4, B9 | DONE | Migration `20260711120000_m3_review_surfaces.sql`: also creates the M3 substrate `person_links` + `merge_log` (B12 deferred them here). `match_review_cards` is a **SECURITY DEFINER + security_barrier** view self-filtered to `can_write_tree`/admin (plan's "security_invoker=on" is incompatible with the M0 admin-only base RLS — DEFINER+barrier+auth.uid() filter is the correct realization). Counterparty detail (initials/district/decade) only for deceased-public **and** only when neither side living; else redacted token; `field_agreement` (agree/disagree/missing, no weights) suppressed for living-involved. `person_identity_groups` matview = union-find (transitive closure, canonical=min uuid via `array_agg(ORDER BY)[1]`) over CONFIRMED links, UNIQUE(person_id) + `refresh_person_identity_groups()`. AFTER UPDATE OF status → `match_audit` (DEFINER trigger, only on real change). pgTAP `m3_review_surfaces_test.sql` (27) green; suite **154/154**. Engine-internal tables not yet app-consumed → types regen deferred to B16/B17. |
| B15 | M3: `confirm/reject/revoke_person_link` + `resolve_match` + `resolve_match_hint` RPCs; revoke = full teardown (plan §6 RPCs) | matching, db | B14 | DONE | Migration `20260711130000_m3_link_rpcs.sql` (all SECURITY DEFINER + `search_path=''`, self-authorizing). `resolve_match(approve/reject/defer)`: **deceased pair → admin fuses directly (confirmed link); any living-involved pair → NEVER admin-fused, routed to dual-owner hints (§5.2 bullet 3); both-living → admin-only (no hint/notify)**. `resolve_match_hint(accept/decline)`: decline is silent (§5.3); both-accept → confirm. `confirm/reject_person_link` (admin). **`revoke_person_link`** (admin OR involved owner) = full teardown: flip+merge_log, retract notifications, expire relay tokens, invalidate cached `match_paths`, and QUEUE (not sync-run) overlay refresh via new deferred `overlay_refresh_queue` + `drain_overlay_refresh()`. Living notifications carry zero detail. pgTAP `m3_link_rpcs_test.sql` (23) — authz boundary, living/deceased fuse rules, dual-owner confirm, revoke teardown+re-isolation — green; suite **177/177**; typecheck/lint/131 tests green. |
| B16 | M3: `data/admin/review.ts` + `/admin/review` queue UI (RTL A-right, agree/disagree/missing chips, live re-derive via score_pair, living badges) using `requireAdmin` (plan §7-M3) | matching, ui | B14, B15, A5 | DONE | `data/admin/review.ts` (`listReviewQueue` admin-gated, sorted by score; `rescoreMatch` live re-derive via `score_pair` RPC; `resolveMatchAction`→`resolve_match` RPC; field agree/disagree/missing derived in TS mirroring `match_field_agreement`). `/admin/review/page.tsx` (isCurrentUserAdmin→notFound) + `AdminReviewClient.tsx` (RTL, bilingual chips, living badges, Merge/Defer/Reject + Re-check, optimistic). **Types gotcha:** `types/database.ts` is HAND-maintained (curated Database + enums + domain interfaces), NOT `supabase gen`-generated — running gen overwrites it and breaks the app; extend it by hand (added `score_pair`/`resolve_match`/`is_person_living` Functions). Gates: typecheck ✅ · lint 0/0 (291) ✅ · 131 tests ✅. Browser: route compiles + middleware gate redirects unauth→`/login?next=/admin/review` (verified via dev logs); interactive click-through is B20's [E2E] scope (browser pane was unresponsive this run). |
| B17 | M3: `data/user/hints.ts` + `/dashboard/connections` (6th DiscoveryTabs lane, masked cards, privacy-hold action) (plan §7-M3) | matching, ui | B14, B15 | TODO | |
| B18 | M3: NotificationBell + in-app notifications wiring (NO email per decision #6) (plan §7-M3) | matching, ui | B14 | TODO | |
| B19 | M3: `compute_degrees` same_as zero-cost hop under §5.1 masking + `match_paths` relation marker + revoke re-isolation test (plan §7-M3 exit) | matching, db | B15 | TODO | |
| A8 | Revive Playwright: rewrite helpers (password login + Inbucket OTP), fix 4 stale specs, add redirect/gate/admin specs; suite green locally (brief §7-A8) | auth, [E2E] | A1, A2, A5 | TODO | Relocated here (2026-07-12) to cluster with the [E2E] tasks at the loop's end, per its own "runs after the Track B M-tasks" note — deps (A1/A2/A5) long DONE; needs local stack (up). |
| B20 | M3: E2E — admin Merge/Reject/Defer, owner Accept/Reject, hostile-counterpart worst case, privacy-hold veto (plan §7-M3 exit) | matching, [E2E] | B16, B17, A8 | TODO | |

## Live-apply ledger

_Migrations applied to the live project (ref `nlufpicjdeeqcgepewdg`) get a row here: migration file · when · advisor check result. Tasks finished locally but not applied stay `DONE (LIVE-APPLY PENDING)` with the exact command._

| Migration | Applied | Advisors |
|---|---|---|
| `20260711000000_matches_rls_admin_only.sql` (B2) | ⏳ PENDING — user must apply | run `get_advisors(security)` after apply |
| `20260711010000_degrees_masking.sql` (B3) | ⏳ PENDING — user must apply | run `get_advisors(security)` after apply; note it clears the match_paths cache |
| `20260711030000_search_master_tree_v2_3_living_mask.sql` (B6b) | ⏳ PENDING — user must apply | run `get_advisors(security)` after apply |

**How to apply the pending migrations to live (`nlufpicjdeeqcgepewdg`):** either (a) tell the loop "auto-apply is fine" and it will run `mcp__supabase__apply_migration` for each, then `get_advisors(security)`; or (b) paste each migration file's SQL into the Supabase dashboard SQL editor; or (c) `supabase link --project-ref nlufpicjdeeqcgepewdg && supabase db push`. All pending migrations are byte-for-byte the committed files under `apps/database/supabase/migrations/`.

## Iteration log

| # | Date | Task | Result | Note |
|---|------|------|--------|------|
| 1 | 2026-07-10 | S0 | DONE | Branch `feat/fix-loop-m0-m3` created; planning docs committed (d870e0c); baseline gates green (typecheck/lint/88 tests). Docker Desktop wouldn't reach ready after 2 launches → B1 + A8 BLOCKED(env: Docker); Track A (A1–A7) proceeds. |
| 2 | 2026-07-10 | A1 | DONE | Open-redirect + silent-error fix. Added `lib/auth/safeRedirect.ts` (+8 vitest cases), wired into callback + confirm routes, callback→auth-code-error on failure, encoded `next` in Login.tsx. Gates: typecheck ✅ · lint 0/0 (280) ✅ · test 96 passed ✅. |
| 3 | 2026-07-10 | A2 | DONE | OTP length fragility. `otpConfig.ts` default 8 + `isValidOtp()`; Signup + LoginOtpTab gates accept 6–8 digits; setup-doc advice corrected (out-of-repo, not committed). Gates: typecheck ✅ · lint 0/0 ✅ · test 101 passed ✅. |
| 4 | 2026-07-10 | A3 | DONE | Password policy. New `lib/auth/passwordPolicy.ts` (min 8, bilingual msg); `security.ts` uses it; `UpdatePassword.tsx` surfaces validation message. +3 tests. Gates: typecheck ✅ · lint 0/0 ✅ · test 104 passed ✅. |
| 5 | 2026-07-10 | A4 | DONE | getUser() caching. New `getCachedLoggedInUserIdOrNull`; 8 uid reads → cached claims (0 network), 2 admin → cached verified helper. Only middleware + verified helper still hit getUser. Person-360 render: 0 auth round-trips. Gates: typecheck ✅ · lint 0/0 ✅ · test 104 ✅. |
| 6 | 2026-07-10 | A5 | DONE | requireAdmin extraction. New `data/admin/requireAdmin.ts` (getAdminUserId/requireAdmin/isCurrentUserAdmin, cached+server-verified); removed duplicated assertAdmin + isCurrentUserAdmin; both call sites migrated. Gates: typecheck ✅ · lint 0/0 ✅ · test 104 ✅. |
| 7 | 2026-07-10 | A6 | DONE | Dead-code + doc drift. Removed signUpAction, signInWithMagicLinkAction, getCachedLoggedInSupabaseUser, NewLogin.tsx; fixed middleware doc (proxy.ts default-deny allowlist). Gates: typecheck ✅ · lint 0/0 ✅ · test 104 ✅. |
| 8 | 2026-07-10 | A7 | DONE | Error-message hygiene. New `lib/auth/authErrors.ts` friendlyAuthError (bilingual, generic fallback); safe-action.ts logs raw + returns friendly. +7 tests. Gates: typecheck ✅ · lint 0/0 ✅ · test 111 passed ✅. |
| 9 | 2026-07-10 | env | CONTINUE | Docker came up (server 29.1.3) + live Supabase MCP access confirmed (PG17, execute_sql reads live objects). B1 + A8 reset BLOCKED→TODO. Track A complete (S0, A1–A7); loop continues into Track B, B1 next. |
| 10 | 2026-07-11 | B1 | DUMP DONE (verify pending) | Background subagent wrote 13/16 migration files before a session restart stopped it; recovered the 3 missing search_master_tree migrations (byte-exact vs live) this session. All 16 committed. `db reset` verification blocked: Docker down post-restart, relaunched & booting. |
| 11 | 2026-07-11 | B1 | DONE ✅ | Docker came up (user cleared the blocking dialog). `db reset` builds all 27 migrations from scratch (exit 0); 4 fns + match_paths + generated cols verified in fresh DB; 456 villages seeded; trigram search works. B1 acceptance MET. Next: B2 (M0 security, `[LIVE-APPLY]`). |
| 12 | 2026-07-11 | B2 | DONE (LIVE-APPLY PENDING) | matches RLS leak fix + pgTAP (46/46 green locally). Local-only mode → migration committed, NOT applied to live; ledger records the pending apply. Next: B3 (degrees masking, also `[LIVE-APPLY]`). |
| 13 | 2026-07-11 | B3 | DONE (LIVE-APPLY PENDING) | Degrees masking: is_person_living + mask_degrees_path + compute_degrees rewrite (structural cache, per-viewer masking). pgTAP 55/55, typecheck green. is_person_living shipped here (B4 no longer needs it). Next: B4 (app_settings, matching_runs, person_privacy_holds). |
| 14 | 2026-07-11 | B4 | DONE | M0 foundation tables (app_settings + kill-switch OFF, matching_runs, person_privacy_holds) + RLS + pgTAP (8). Suite 63/63. Next: B5 (CI — remove upstream guard, add pgTAP job, wire test-db into turbo). |
| 15 | 2026-07-11 | B5 | DONE | CI: pgTAP now runs on the fork (new unguarded `pgtap` job → `pnpm run test:db`); stale e2e disabled until A8; test-db wired into turbo. YAML valid, test:db 63/63. Next: B6 (mask living-person birth-year/origin in public-tree search, plan §5.5) — finishes M0. |
| 16 | 2026-07-11 | B6 | DONE | Public "List of all people" (getTreePeoplePage) hides living-person birthYear/birthPlaceAr from non-privileged viewers. +3 tests, 114 green. Search-RPC surface split into B6b (follow-up). **M0 core complete (B1–B6).** Next: B6b, then M1 (B7 feature layer). |
| 17 | 2026-07-11 | B6b | DONE (LIVE-APPLY PENDING) | search_master_tree v2_3 masks living-person birth/origin for non-privileged searchers; pgTAP 70/70. **M0 fully complete incl. §5.5.** Next: M1 — B7 (match_features + dirty queue + triggers). |
| 18 | 2026-07-11 | B7 | DONE | M1 feature layer: match_features + match_features_dirty (deny-all RLS) + enqueue + 6 derivation triggers. pgTAP 76/76. refresh_match_features deferred to B8/B9. Next: B8 (transliterate_to_arabic + name_variants + refresh_match_features). |
| 19 | 2026-07-11 | B8 | DONE | Cross-script transliterate_to_arabic (Ibrahim/Abraham→ابراهيم) + name_variants dictionary. pgTAP 83/83. refresh_match_features split to B8b. Next: B8b (refresh_match_features), then B9 (score_pair). |
| 20 | 2026-07-11 | B8b | DONE | refresh_match_features + match_resolve_parents (resolveParents parity) + fold helpers. Feature layer fully functional. pgTAP 91/91. Next: B9 (score_pair — the 25-param scorecard w/ collapse, cluster gate, disagreement vetoes). |
| 21 | 2026-07-11 | B9 | DONE | score_pair CORE: 25 params + literal/phonetic collapse + partial credit + hard vetoes + PII-min breakdown. **The engine can now score pairs.** pgTAP 98/98. Refined cluster/disagreement gate split to B9b. Next: B9b, then B10 (fixtures + parity). |
| 22 | 2026-07-11 | B9b | DONE | score_pair refined gate: clusters + score_pct + 5 disagreement vetoes + decision. **M1 scorer complete.** pgTAP 104/104. Next: B10 (synthetic fixtures + Vitest parity), then M2 (shadow batch — first demoable matcher). |
| 23 | 2026-07-11 | B10 | DONE | Eval fixtures (resolveParents U/X + same-gender, cross-script scoring) + JS↔SQL transliterate parity (caught+fixed Khalil→علي bug in phonetic.ts). pgTAP 109/109, Vitest 131/131. **★ M1 COMPLETE ★** Next: M2 — B11 (blocking: match_block_keys + 7 passes + generate_match_candidates). |
| 24 | 2026-07-11 | B11 | DONE | M2 blocking: match_block_keys + match_block_skips + 6 passes + generate_match_candidates (cross-tree, skew-capped, privacy-hold excluded). pgTAP 115/115. Next: B12 (run_matching_batch — shadow runner, first demoable matcher). |
| 25 | 2026-07-11 | B12 | DONE | **★ SHADOW MATCHER LIVE ★** run_matching_batch end-to-end: refresh→block→candidates→score→UPSERT matches (pending), never clobbers human decisions, matching_runs counters. pgTAP 121/121. The engine now finds duplicate people across trees. Next: B13 (eval schema + run_eval) finishes M2. |
| 26 | 2026-07-12 | B13 | DONE | Eval harness: eval_pairs + eval_runs + run_eval (score→sweep→precision/recall/F1/cross_script_recall). pgTAP 127/127. **★ M2 COMPLETE ★** (blocking + shadow batch + eval). Next: M3 — B14 (review tables + Mother Tree overlay matview). |
| 27 | 2026-07-12 | B14 | DONE | M3 review surfaces: person_links + merge_log substrate, match_audit (+status trigger), notifications, contact_relay, match_hints, **match_review_cards** (DEFINER+barrier masked card — reveal only deceased-public & neither-living, else redacted; field_agreement suppressed for living), **person_identity_groups** matview (union-find over confirmed links = Mother Tree overlay) + unique index + refresh helper, v_match_explanations (admin/deceased-public). pgTAP 154/154; typecheck/lint/tests green. Next: B15 (link + resolve RPCs). |
| 28 | 2026-07-12 | B15 | DONE | Link/resolve RPCs: resolve_match (deceased→admin-fuse; living→dual-owner only; both-living→admin-only), resolve_match_hint (silent decline, dual-accept→confirm), confirm/reject/revoke_person_link. **revoke = full teardown** (retract notifications, expire relay, invalidate match_paths, queue overlay refresh via overlay_refresh_queue + drain). Living notifications zero-detail. pgTAP 177/177; typecheck/lint/131 green. Next: B16 (`/admin/review` UI — first M3 UI task; needs types regen). |
| — | 2026-07-12 | board | — | Relocated A8 (E2E) row to the end-of-loop [E2E] cluster (next to B20) so the mechanical "first TODO" pick is unambiguous. Commit c78de60. |
| 29 | 2026-07-12 | B16 | DONE | Admin review UI: `data/admin/review.ts` (listReviewQueue/rescoreMatch/resolveMatchAction) + `/admin/review` page + `AdminReviewClient` (RTL bilingual chips, living badges, Merge/Defer/Reject/Re-check). Discovered `types/database.ts` is HAND-maintained → extended by hand (score_pair/resolve_match/is_person_living). Gates green (typecheck/lint 0-0/131). Route+auth-gate verified via dev logs (unauth→/login?next=/admin/review); interactive verify deferred to B20 (browser pane unresponsive). Next: B17 (`/dashboard/connections` owner lane). |

## Progress summary

**Session 1 (2026-07-10) — Track A (auth) complete; Docker + live DB came up mid-session, loop continuing into Track B.**

### Shipped (branch `feat/fix-loop-m0-m3`, 8 commits, all gates green — 111 unit tests)
- **S0** bootstrap: branch + planning docs + baseline recorded.
- **A1** — closed the unauthenticated **open redirect** in the OAuth callback (+ silent-error swallow); shared `sanitizeNextPath` guards both auth routes; `next` encoded in Login. (8 tests)
- **A2** — fixed the **OTP lockout**: client accepts any 6–8 digit code, default length 8. (5 tests)
- **A3** — password policy **min(4)→min(8)** with a bilingual error. (3 tests)
- **A4** — removed **10 uncached `getUser()`** round-trips; person-360 render now does 0 auth round-trips; admin/password stay server-verified.
- **A5** — single **`requireAdmin()`** helper (`data/admin/requireAdmin.ts`); both duplicate admin checks migrated.
- **A6** — deleted **~250 lines of dead auth code** (2 actions, `NewLogin.tsx`, a stale cached helper) + fixed the middleware doc drift.
- **A7** — **error-message hygiene**: `friendlyAuthError` bilingual mapper, no raw Supabase/DB internals reach the client. (7 tests)
- Out-of-repo doc `SUPABASE_OTP_SETUP.md` (ancestry root) updated in place (not committed).

### Environment (updated mid-session)
- **Docker Desktop is now up** (came ready ~50 min after launch) and **live Supabase MCP access is confirmed** (project `nlufpicjdeeqcgepewdg`, PG17). Both of B1's prerequisites are satisfied.
- **No live-DB mutations have occurred** (LIVE-APPLY ledger empty). B1 is read-only on live (introspection) + local file/db-reset.

### Track B — B1 foundation VERIFIED (2026-07-11); into M0 security next
- **B1 DONE + verified** — `db reset` builds all 27 migrations from scratch; search/degrees objects confirmed in a fresh local DB; 456 villages seeded. Docker is finally up and stable.
- **Next: B2** — first `[LIVE-APPLY]` task (replace the leaky `matches_select` RLS policy with admin-only + pgTAP). Per brief §2.2, `[LIVE-APPLY]` tasks do LOCAL verification first, then apply to the live production DB (`nlufpicjdeeqcgepewdg`) via MCP, then `get_advisors(security)` check. These change the live database — the loop does the local work first and treats the live apply as the gated step.
- Then B3 (degrees masking, `[LIVE-APPLY]`), B4/B5/B6 (M0 foundations), M1→M3, and A8 E2E.
- **Docker caveat:** it dies on session restarts and boots very slowly here (often stuck on a GUI dialog). Keep Docker Desktop up between iterations for Track B to flow.

### Recommended human review any time
- The 9 commits on `feat/fix-loop-m0-m3` so far (S0 + 7 auth fixes) are self-contained and independently valuable. The branch is intentionally **not pushed and not merged to main** by the loop — review/push/PR when satisfied.
