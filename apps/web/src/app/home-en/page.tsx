import { HomeEnClient } from './HomeEnClient';

export const metadata = {
  title: 'Palestinian Roots Platform | Juthoor — English Preview',
  description:
    'One family tree for all Palestinians, everywhere. Preview of an English, Appendix-2-structured homepage for the Palestinian Roots Platform.',
};

export default function HomeEnPreviewPage() {
  return <HomeEnClient />;
}
