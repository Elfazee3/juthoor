import { AboutClient } from './AboutClient';

export const metadata = {
  title: 'عن جذور — About Juthoor',
  description: 'Why Juthoor exists — a non-profit mission to reconnect Palestinian families through one unified, intelligent family tree.',
};

export default function AboutPage() {
  return <AboutClient />;
}
